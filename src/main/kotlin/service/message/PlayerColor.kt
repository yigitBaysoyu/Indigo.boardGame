package service.message

/**
 * The given Color for each Player. displayed on the Gates that the player partially or completely owns.
 */
enum class PlayerColor {
    WHITE,
    RED,
    BLUE,
    PURPLE
}